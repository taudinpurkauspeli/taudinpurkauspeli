var app = angular.module('diagnoseDiseases');

app.controller("TasksCompletedSubtzaskController", [
    "$scope", "$stateParams", "LocalStorageService",
    function($scope, $stateParams) {

    }
]);