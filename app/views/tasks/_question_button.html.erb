<%= form_for(@new_asked_question) do |f| %>
	<%= f.hidden_field :question_id, :value => question.id %>
	<%= f.hidden_field :user_id, :value => current_user.id %>
	<%= f.submit question.title, :class => 'btn btn-default btn-save widthAuto' %>
<% end %>