<div class="row">
  <div class="col-md-9">
    <input class="btn btn-default btn-hypothesis-group multiline" type="button" value="<%= group.name %>" data-toggle="collapse" data-target="#collapseHypothesisGroup<%= group.id%>" />
  </div>
  <div class="col-md-3">
    <%= button_to 'Poista', group, method: :delete, :class => 'btn btn-default btn-del', data: { confirm: 'Oletko varma?' } %>
  </div>
</div>