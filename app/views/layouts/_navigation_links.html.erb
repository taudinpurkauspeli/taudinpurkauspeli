<%# add navigation links to this file %>
<li role="presentation" class="active"><a href="#anamnesisTab" aria-controls="home" role="tab" data-toggle="tab">Anamneesi</a></li>
<li role="presentation"><a href="#taskTab" aria-controls="tasks" role="tab" data-toggle="tab">Toimenpiteet</a></li>
<li role="presentation"><a href="#hypothesisTab" aria-controls="hypotheses" role="tab" data-toggle="tab">Työhypoteesit</a></li>
<% unless current_task.nil? || current_user_has_completed_task(current_task.id) %>
	<li role="presentation"><a href="#currentTask" aria-controls="currentTask" role="tab" data-toggle="tab"><%= current_task.name %></a></li>
<% end %>
<!--
<%= link_to 'Anamneesi', current_exercise, class: if current_page?(current_exercise) then 'active navbar-brand' else 'navbar-brand' end %>

<%= link_to 'Toimenpiteet', tasks_path, class: if current_page?(tasks_path) then 'active navbar-brand' else 'navbar-brand' end %>

<%= link_to 'Työhypoteesit', hypotheses_path, class: if current_page?(hypotheses_path) then 'active navbar-brand' else 'navbar-brand' end %>

<%#
  Only show the current task tab if both of these is true:
      1. Current task is set in session
      2. User has not completed current task'
%>
<% unless current_task.nil? || current_user_has_completed_task(current_task.id) || current_user_is_admin %>
  <%= link_to current_task.name, current_task, class: if current_page?(current_task) then 'active navbar-brand' else 'navbar-brand' end %>
<% end %>
-->